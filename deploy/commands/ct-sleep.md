---
description: Cortex — lie down until a given time.
---

$ARGUMENTS is either HH:MM or a number of minutes. Run `~/CC-Lab/cortex/.venv/bin/python -m cortex.ctl sleep --until $ARGUMENTS` if it looks like HH:MM, else `~/CC-Lab/cortex/.venv/bin/python -m cortex.ctl sleep --min $ARGUMENTS` if it's a bare number. Report the one-line output.
