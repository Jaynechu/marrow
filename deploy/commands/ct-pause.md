---
description: Cortex — DND on.
---

Run `~/CC-Lab/cortex/.venv/bin/python -m cortex.ctl pause` via Bash and report its one-line output. Unpause later with `/ct-wake` or `python -m cortex.ctl resume`.
